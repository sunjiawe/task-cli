from utils.call_llm import call_llm

def decompose_requirement(requirement, project_context):
    """Decomposes a requirement into tasks using an LLM."""
    # This is a placeholder for the actual LLM call
    print(f"Decomposing requirement: {requirement} with context: {project_context}")
    return []

def get_task_advice(task, project_context):
    """Gets advice for a task using an LLM."""
    # This is a placeholder for the actual LLM call
    print(f"Getting advice for task: {task} with context: {project_context}")
    return "Just do it!"

def generate_report(project_context):
    """Generates a report using an LLM."""
    # This is a placeholder for the actual LLM call
    print(f"Generating report with context: {project_context}")
    return "This is a report."

def answer_question(question, project_context):
    """Answers a question using an LLM."""
    # This is a placeholder for the actual LLM call
    print(f"Answering question: {question} with context: {project_context}")
    return "This is the answer."
